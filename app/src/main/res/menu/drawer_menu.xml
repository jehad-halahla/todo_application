<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Tasks Section -->
    <group android:id="@+id/group_tasks" android:checkableBehavior="none">
        <item
            android:id="@+id/nav_today"
            android:title="Today"
            android:icon="@drawable/ic_baseline_today_24" />
        <item
            android:id="@+id/nav_new_task"
            android:title="New Task"
            android:icon="@drawable/ic_baseline_add_task_24" />
        <item
            android:id="@+id/nav_all"
            android:title="All"
            android:icon="@drawable/ic_baseline_density_small_24" />
        <item
            android:id="@+id/nav_completed"
            android:title="Completed"
            android:icon="@drawable/ic_baseline_done_all_24" />
        <item
            android:id="@+id/nav_search"
            android:title="filter"
            android:icon="@drawable/ic_baseline_content_paste_search_24" />
    </group>

    <!-- Profile Section -->
    <group android:id="@+id/group_profile" android:checkableBehavior="none">
        <item
            android:id="@+id/nav_profile"
            android:title="Profile"
            android:icon="@drawable/ic_baseline_person_24" />
    </group>

    <!-- Logout Section -->
    <group android:id="@+id/group_logout" android:checkableBehavior="none">
        <item
            android:id="@+id/nav_logout"
            android:title="Logout"
            android:icon="@drawable/ic_baseline_logout_24" />
    </group>
</menu>
